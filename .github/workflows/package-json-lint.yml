name: Package JSON Lint

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  package-json-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: yarn global add npm-package-json-lint@9.0.0
      - run: npmPkgJsonLint .
